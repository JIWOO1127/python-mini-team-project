# EV Diag Final 최종 사용 매뉴얼

## 1. 지원 환경

- Windows 10/11
- Python 3.13.x (권장 3.13.15)
- 활성 모델: LightGBM `lightgbm-ver2`
- 모델 관련 버전: LightGBM 4.7.0, scikit-learn 1.7.2, joblib 1.5.3

이 프로젝트의 `.venv`는 생성된 컴퓨터와 경로에 종속됩니다. 다른 컴퓨터에서 `.venv` 폴더를 복사하지 말고 반드시 새로 생성합니다.

## 정상 실행 순서 한눈에 보기

정상적인 경우에는 **1절 → 아래 import 확인 → 2절** 순서로 진행합니다.

1. 1절에서 Python 3.13 가상환경과 패키지를 준비합니다.
2. `.env`에 API 키와 백엔드 주소를 입력합니다.
3. 아래 import 확인 명령이 성공하는지 확인합니다.
4. 2절의 `03_프로그램실행.cmd`로 앱을 실행합니다.

**1-1절은 Python/NumPy import 오류가 발생했을 때만 수행하는 복구 절차입니다.

## 1. 최초 환경 설정

가상환경을 만들 때는 `py`의 기본 버전에 의존하지 말고 Python 3.13을 명시합니다.

```powershell 을 관리자 권한으로 실행하고 아래 코드를 입력한다.(폴더의 경로는 수정 필요)
cd "C:\Users\한국전파진흥협회\Desktop\DA\For Share\EV_Diag_Final"
py -3.13 --version
py -3.13 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
Copy-Item .env.example .env
```

`py -3.13`을 찾을 수 없다면 Python 3.13을 설치한 뒤 다시 실행합니다.

`.env`에는 다음 값을 설정합니다.

```dotenv
KAKAO_REST_API_KEY=발급받은_키
KMA_SERVICE_KEY=발급받은_키
BACKEND_BASE_URL=http://127.0.0.1:8000
```

배터리 진단과 대시보드는 API 키 없이 실행할 수 있지만 충전소·서비스센터 검색에는 해당 키가 필요합니다.

키를 입력한 다음에는 2-1절로 가지 말고 아래 명령으로 설치 상태를 확인한 뒤 3절의 일반 사용자 실행으로 이동합니다.

```powershell
.\.venv\Scripts\python.exe --version
.\.venv\Scripts\python.exe -c "import numpy, pandas, lightgbm; print('dependencies ok')"
```

### 1-1. 오류 발생 시: NumPy/Pandas import 복구

이 절은 **2절 환경 설정 중 Python/NumPy import 오류가 발생했을 때만** 수행합니다.

다음과 같은 오류가 발생하면 패키지 하나만 재설치하지 말고 가상환경을 새로 만듭니다.

```text
The Python version is: Python 3.10
_multiarray_umath.cp313-win_amd64.pyd
ModuleNotFoundError: No module named 'numpy._core._multiarray_umath'
```

이는 Python 3.10 가상환경 안에 Python 3.13용 NumPy 바이너리가 섞인 상태입니다. PowerShell에서 기존 환경을 삭제하지 않고 이름을 바꿔 보관한 뒤 재생성합니다.

```powershell
cd "C:\Users\한국전파진흥협회\Desktop\DA\For Share\EV_Diag_Final"
Get-Content .venv\pyvenv.cfg
Rename-Item .venv .venv_py310_backup_20260828
py -3.13 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

재생성 후 다음 두 명령에서 Python 3.13과 NumPy import 성공을 확인합니다.

```powershell
.\.venv\Scripts\python.exe --version
.\.venv\Scripts\python.exe -c "import numpy, pandas, lightgbm; print('dependencies ok')"
```

`pyvenv.cfg`의 `version`이 3.10이거나 `python.exe --version`이 3.10이면 아직 잘못된 환경입니다. `py -3.13 --version` 자체가 실패하면 Python 3.13 설치가 먼저 필요합니다.

복구가 끝나면 이 절차를 반복하지 말고 **3절 일반 사용자 실행**으로 이동합니다.

## 2. 정상 실행: 일반 사용자

환경 설정, `.env` 입력, import 검증을 모두 완료한 뒤 `03_프로그램실행.cmd`를 실행합니다.

```powershell
.\.venv\Scripts\python.exe run_app.py
```

또는 파일 탐색기에서 `03_프로그램실행.cmd`를 실행합니다. 런처는 다음 순서로 동작합니다.

1. `BACKEND_BASE_URL/health` 확인
2. 정상 로컬 서버가 없으면 백엔드 자동 시작
3. 서버 준비 완료 후 Tkinter UI 실행
4. UI 종료 시 런처가 시작한 서버만 종료

이미 정상 실행 중인 서버나 원격 `BACKEND_BASE_URL`은 재사용하며 런처가 종료하지 않습니다. 로컬 서버의 `active_model`이 현재 manifest의 `lightgbm-ver2`와 다르면 재사용하지 않습니다.

## 3. 개발자용 서버 단독 실행

서버 로그를 별도 창에서 확인하거나 UI 없이 API만 실행할 때 사용합니다.

```powershell
.\.venv\Scripts\python.exe -m uvicorn src.backend.main:app --host 127.0.0.1 --port 8000
```

또는 `02_서버실행.cmd`를 실행합니다.

- 상태 확인: `http://127.0.0.1:8000/health`
- API 문서: `http://127.0.0.1:8000/docs`

## 4. 모델과 데이터

활성 모델과 경로는 `models/model_manifest.json`이 결정합니다.

- 모델: `models/lightgbm-ver2/lgbm_battery_model.pkl`
- 차량 입력: `data/test/lightgbm-ver2/vehicles.csv`
- 표시용 메타데이터: `data/test/default/vehicles.csv`
- 피처 profile: `data/test/lightgbm-ver2/feature_profile.json`

화면의 **테스트 차량 ID 자동 입력** 버튼은 위 활성 테스트 데이터셋에서 매번 차량 ID를 무작위로 요청해 입력합니다. 입력된 ID는 그대로 진단 버튼에 사용할 수 있습니다.

프로그램 실행에 필요한 것은 활성 모델 번들과 테스트용 데이터입니다. 학습에 사용한 원본 데이터는 예측 실행에 필요하지 않으므로 배포본에 포함하지 않아도 됩니다.

GitHub로 공유할 때는 다음 런타임 파일을 함께 커밋해야 합니다.

- `models/model_manifest.json`
- `models/lightgbm-ver2/lgbm_battery_model.pkl`
- `data/test/lightgbm-ver2/vehicles.csv`
- `data/test/lightgbm-ver2/feature_profile.json`
- `data/test/default/vehicles.csv`

루트의 `models/battery_failure_model.pkl`이나 `models/default/`, `models/reviewed-xgb-3.2.0/`의 과거 모델은 현재 진단 경로에서 사용하지 않습니다. 현재 `.gitignore`에서 모델·테스트 CSV를 제외하는 저장소라면 위 활성 파일만 `git add -f`로 추가하거나 Git LFS로 관리합니다. 파일이 빠진 clone에서는 manifest 오류 또는 테스트 차량 조회 오류가 발생합니다.

모델·CSV·manifest·profile을 교체하면 서버를 종료하고 다시 실행합니다. `train_default_model.py`가 생성하는 XGBoost 번들은 `models/generated-xgb/`에만 저장되며 활성 LightGBM 모델을 자동 변경하지 않습니다.

## 5. 테스트

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

테스트는 외부 API를 호출하지 않으며, 활성 LightGBM 번들 스모크 테스트를 포함합니다.

## 6. 문제 해결

| 증상 | 원인 및 해결 |
| --- | --- |
| NumPy `_multiarray_umath` 오류 | Python 3.10과 3.13 패키지가 섞인 상태입니다. 2-1절의 `.venv` 보관·재생성 절차를 수행합니다. |
| `.venv` Python이 3.10으로 표시됨 | `py -3.13 -m venv .venv`로 새로 생성합니다. |
| `No module named 'src'` | 프로젝트 루트에서 실행합니다. |
| 포트 8000 사용 중 | 기존 서버를 종료하거나 `.env`의 `BACKEND_BASE_URL` 포트를 변경합니다. |
| 다른 `active_model` 오류 | 구버전 서버가 실행 중입니다. 해당 서버를 종료한 뒤 `03_프로그램실행.cmd`를 다시 실행합니다. |
| 원격 백엔드 연결 실패 | `BACKEND_BASE_URL`과 원격 서버의 `/health` 응답을 확인합니다. |
| Kakao/KMA 오류 | `.env`의 API 키를 확인한 뒤 서버를 재시작합니다. |
| 차량 ID 조회 실패 | `data/test/lightgbm-ver2/vehicles.csv`의 `vehicle_id`를 확인합니다. |
| 모델 manifest 오류 | manifest의 모델·CSV·profile 경로와 피처 목록을 확인합니다. |

## 8. 현재 서버가 남아 있는 경우

8000번 포트에 다른 프로젝트의 서버가 실행 중이면 새 런처가 이를 현재 앱으로 인정하지 않습니다. 기존 서버를 종료하거나 다음처럼 포트를 바꿉니다.

```dotenv
BACKEND_BASE_URL=http://127.0.0.1:18000
```

그 후 `03_프로그램실행.cmd`를 다시 실행합니다.
