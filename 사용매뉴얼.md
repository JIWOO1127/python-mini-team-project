# EV Diag Final 사용 매뉴얼

## 권장 실행 방법: Python 직접 명령

PowerShell 실행 정책이나 배치 파일 인코딩 문제를 피하기 위해, 아래 매뉴얼의 **Python 직접 명령**을 기본 방법으로 사용합니다. `.ps1`과 `.cmd` 파일은 편의용 보조 방법입니다.

실행 정책이 허용된 환경에서는 아래 보조 파일도 사용할 수 있습니다.

- `01_환경설정.cmd` — 최초 한 번만 실행
- `02_서버실행.cmd` — 첫 번째 창에서 실행 후 유지
- `03_프로그램실행.cmd` — 새 창에서 실행

## 1. 최초 실행(한 번만): Python 직접 명령

PowerShell을 열고 아래 폴더로 이동합니다.

```powershell
cd "C:\Users\한국전파진흥협회\Desktop\DA\For Share\EV_Diag_Final"
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

`py` 명령을 찾지 못하면 첫 줄만 아래처럼 바꿉니다.

```powershell
python -m venv .venv
```

`01_환경설정.cmd` 또는 `01_환경설정.ps1`도 같은 작업을 수행합니다. `.ps1`은 실행 정책이 허용될 때만 사용합니다.

### LightGBM ver2 업데이트 후 필요한 작업

이미 환경 설정을 마친 경우에도 LightGBM과 모델 저장 버전에 맞는 scikit-learn을 추가해야 합니다. 아래 명령을 한 번 실행합니다.

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

`.env`가 없으면 이 과정에서 `.env.example`을 복사해 만듭니다. 아래 값을 확인합니다.

```dotenv
KAKAO_REST_API_KEY=발급받은_키
KMA_SERVICE_KEY=발급받은_키
BACKEND_BASE_URL=http://127.0.0.1:8000
```

## 2. 서버 실행

첫 번째 PowerShell 창에서 실행합니다. 이 명령은 실행 정책과 무관합니다.

```powershell
cd "C:\Users\한국전파진흥협회\Desktop\DA\For Share\EV_Diag_Final"
.\.venv\Scripts\python.exe -m uvicorn src.backend.main:app --host 127.0.0.1 --port 8000
```

다음 문구가 나타나면 정상입니다. 이 창은 종료하지 않습니다.

```text
Uvicorn running on http://127.0.0.1:8000
```

브라우저에서 `http://127.0.0.1:8000/health`를 열어 `"status":"ok"`를 확인할 수 있습니다.

## 3. Tkinter 프로그램 실행

새 PowerShell 창에서 실행합니다. 이 명령은 실행 정책과 무관합니다.

```powershell
cd "C:\Users\한국전파진흥협회\Desktop\DA\For Share\EV_Diag_Final"
.\.venv\Scripts\python.exe -m src.ui
```

`.ps1` 파일을 사용해야 한다면 해당 PowerShell 창에서 먼저 아래를 실행합니다.

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
```

그 뒤 정확한 파일명인 `.\02_서버실행.ps1`, `.\03_프로그램실행.ps1`을 실행합니다. `..venv`처럼 점 두 개를 쓰면 안 됩니다.

## 4. 테스트 차량 데이터 교체

현재 FastAPI 서비스가 읽는 파일은 아래 한 개입니다.

```text
data\test\lightgbm-ver2\vehicles.csv
```

`data\test\default\vehicles.csv`는 제조사·모델·배터리 표시를 위한 ID 메타데이터 파일입니다. `data\vehicle_samples\vehicles.csv`는 이전 분석 코드용 호환 파일이므로, 이 파일만 교체하면 현재 앱 예측에는 반영되지 않습니다.

새 CSV에는 아래 항목이 필요합니다.

- 필수: `vehicle_id`와 모델 입력 변수 18개
- 화면 차량 정보 표시용: `vehicle_brand`, `vehicle_model`, `battery_chemistry`
- `battery_failure`는 예측 실행에는 필수가 아니지만 테스트 데이터 검증용으로 유지하는 것을 권장합니다.

CSV, 모델 파일, `model_manifest.json`, `feature_profile.json` 중 하나라도 변경했다면 **서버를 반드시 종료 후 다시 실행**해야 합니다. 서버가 차량 CSV와 모델을 메모리에 캐시하기 때문입니다. 안전한 순서는 다음과 같습니다.

1. 서버 창에서 `Ctrl + C`를 눌러 서버 종료
2. `data\test\default\vehicles.csv`를 새 파일로 교체
3. 첫 번째 PowerShell에서 서버 직접 명령 또는 `02_서버실행.cmd` 재실행
4. 이미 열려 있는 Tkinter 창도 닫고 직접 UI 명령 또는 `03_프로그램실행.cmd`으로 다시 실행

## 5. 자주 발생한 문제

| 증상 | 원인 | 해결 |
| --- | --- | --- |
| `No module named 'src'` | 프로젝트 폴더 밖에서 서버 실행 | `EV_Diag_Final` 폴더로 이동 후 서버 직접 명령 실행 |
| 스크립트 실행 차단 | PowerShell 실행 정책 | `.cmd` 파일 또는 Python 직접 명령 사용. 필요 시 `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force` 실행 |
| `.venv` 또는 `uvicorn`을 찾지 못함 | 최초 환경 설정 미실행 또는 `..venv` 오타 | `01_환경설정.cmd` 또는 Python 직접 설치 명령 실행 후 `.` + `\.venv` 형식 사용 |
| 8000 포트 사용 중 | 이전 서버가 아직 실행 중 | 이전 서버 창에서 `Ctrl + C` 후 다시 실행 |
| `KAKAO_REST_API_KEY` 오류 | `.env`에 키가 없거나 서버가 옛 `.env`를 읽음 | 패키지 루트 `.env`에 키를 넣고 서버 재시작 |
| 차량 ID는 예측되지만 제조사·모델·배터리가 비어 있음 | 앱이 읽는 CSV에 표시 열이 없거나 다른 CSV를 교체함 | `data\test\default\vehicles.csv`에 `vehicle_brand`, `vehicle_model`, `battery_chemistry` 열 확인 후 서버 재시작 |
| Nissan 센터가 비어 있음 | 기존 서버가 수정 전 코드·캐시를 사용 | 서버를 재시작하고 다시 검색 |

## 6. 참고

이 프로그램은 로컬 FastAPI 서버와 Tkinter 앱을 함께 사용합니다. GitHub에 코드가 있어도 별도 배포 서버가 자동으로 열리는 것은 아니므로, 사용할 때마다 2단계 서버 실행이 필요합니다.
